* Para multiplicar, contamos con el _operador_ `*`.
* El cuadrado de un número se lo obtiene multiplicando al número por sí mismo.
  * Otra forma, es utilizando el operador `^`, que eleva un número a una potencia. 